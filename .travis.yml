deploy:
  provider: azure_web_apps
  username: azure_deployment_user       # If AZURE_WA_USERNAME isn't set
  site: travis_react       # If AZURE_WA_SITE isn't set
  #slot: azure_deployment_slotname       # (optional) If AZURE_WA_SLOT isn't set
